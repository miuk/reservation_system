substitutions:
  _REGION: asia-northeast1
  _SERVICE_NAME: reservation-backend
  _FIREBASE_PROJECT_ID: ${PROJECT_ID}
  _FIRESTORE_DATABASE_ID: (default)
  _FRONTEND_ORIGIN: http://localhost:5173
  _ADMIN_EMAILS: admin@example.com
  _SERVICE_ACCOUNT: ""

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/cloud-run-source-deploy/${_SERVICE_NAME}:${SHORT_SHA}
      - .

  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/cloud-run-source-deploy/${_SERVICE_NAME}:${SHORT_SHA}

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE_NAME}
      - --image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/cloud-run-source-deploy/${_SERVICE_NAME}:${SHORT_SHA}
      - --region=${_REGION}
      - --platform=managed
      - --no-allow-unauthenticated
      - --ingress=internal-and-cloud-load-balancing
      - --port=8080
      - --set-env-vars=FIREBASE_PROJECT_ID=${_FIREBASE_PROJECT_ID},FIRESTORE_DATABASE_ID=${_FIRESTORE_DATABASE_ID},FRONTEND_ORIGIN=${_FRONTEND_ORIGIN},ADMIN_EMAILS=${_ADMIN_EMAILS}
      - --service-account=${_SERVICE_ACCOUNT}

images:
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/cloud-run-source-deploy/${_SERVICE_NAME}:${SHORT_SHA}
