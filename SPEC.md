# 会議室予約システム

## 概要

* 単一の会議室を予約するシステム
* 利用者は Google アカウントでログインする
* 管理者が登録した利用者のみログイン後の機能を利用できる
* 予約申込は仮予約として登録され、管理者の承認により予約確定となる

## 予約対象

* 予約対象の会議室は1つ
* 予約対象名は backend 環境変数で設定可能
  * 未設定時は `会議室`
* 予約コマは下記の3コマ
  * 午前
  * 午後
  * 夜

## 予約画面

* カレンダーを表示する
* 予約したいコマをクリックして選択する
* 予約済みのコマはクリックできない
* 予約済みのコマには団体名と状態を表示する
  * 仮予約
  * 予約確定
* 空きコマには午前、午後、夜を表示する
* 申込日から何か月後まで予約可能かは backend 環境変数で設定可能
  * 未設定時は6か月後まで
* 一度に予約可能なコマ数は backend 環境変数で設定可能
  * 未設定時は50コマまで
* 申込内容エリアに選択中のコマ一覧を表示する
* 選択中のコマ一覧からコマ選択を解除できる
* 予約画面の申込一覧には、自分の申込のみ表示する
  * 管理者でログインしている場合でも、予約画面では他利用者の申込を表示しない

## 予約申込入力項目

* 団体名 (必須)
* 代表者名、代表者の連絡先電話番号、代表者の email (必須)
* 代表者名2、代表者2の連絡先電話番号、代表者2の email (任意)
* 利用予定人数 (必須)
* 利用目的 (必須)
* その他連絡事項 (任意)

## 予約状態

* 申込をすると仮予約状態になる
* 管理者が承認すると予約確定状態になる
* 管理者は予約を取り消しできる
* 取り消された予約のコマは再度予約可能になる

## 管理者画面

* 予約画面と管理者画面は別画面にする
* 管理者権限を持つ利用者のみ管理者画面を利用できる
* 管理者画面には下記の機能を持つ
  * 予約管理
  * 利用者管理
  * カレンダー印刷
  * 予約データ管理

### 予約管理

* 全利用者の申込を表示する
* 申込内容に下記を表示する
  * 団体名
  * 予約状態
  * 予約コマ一覧
  * 代表者名、電話番号、email、利用予定人数
  * 代表者2の情報。ただし登録がある場合のみ
  * 利用目的
  * 申込日時
  * 予約確定日時
  * 取消日時
  * 予約確定操作をした管理者
  * 取消操作をした管理者
* 仮予約は承認できる
* 仮予約、予約確定の予約は取り消しできる
* 予約確定後も取消ボタンを右端に表示する

### 利用者管理

* 管理者はログイン可能な利用者を登録できる
* 利用者には下記を設定できる
  * email
  * 名前
  * 権限
    * 利用者
    * 管理者
  * 有効/無効
* 無効な利用者はログイン後の機能を利用できない
* 初期管理者は環境変数で指定する
* 初期管理者は利用者管理に未登録でも管理者としてログインできる

### カレンダー印刷

* 管理者画面からカレンダーを印刷できる
* 印刷はモノクロにする
* 印刷する期間は、指定月の第一火曜日を含む週の日曜日から始まる6週間とする
* 印刷時のコマ表示は下記とする
  * 予約が無いコマは `-`
  * 予約があるコマは団体名のみ
* 印刷時のコマ内容はセンタリングする

### 予約データ管理

* 管理者は予約データを JSON ファイルとしてエクスポートできる
* 管理者は JSON ファイルから予約データをインポートできる
* インポート方式は下記から選択できる
  * 既存データに追加
  * 既存データを置き換え
* 既存データに追加する場合、既存予約と同じコマを占有するデータはインポートできない
* 既存データを置き換える場合、既存の予約データと予約コマ占有データを削除してからインポートする
* 管理者は予約データを全消去できる
* 管理者は指定日付以前の予約データを消去できる
  * 予約の全コマが指定日付以前の場合のみ、その予約を削除対象にする
  * 複数日にまたがる予約の一部コマだけを削除することはしない
* 破壊的な操作は実行前に確認する

## 認証・認可

* Firebase Authentication の Google ログインを使用する
* backend は Firebase ID token を検証する
* 登録済み利用者、または初期管理者以外は API を利用できない
* 管理者 API は管理者のみ利用できる

## データ

* DB は Firestore を使う
* 主なコレクション
  * `reservations`
    * 申込単位の予約情報
  * `reservationSlots`
    * コマ単位の占有情報
    * 重複予約防止に使う
    * カレンダー表示用に団体名を保持する
  * `allowedUsers`
    * ログイン可能な利用者と管理者を保持する

## 環境・デプロイ

* frontend, backend はコンテナ上で動かす
* backend は下記の環境変数でアプリ設定を変更できる
  * `RESOURCE_NAME`
    * 画面に表示する予約対象名
    * 未設定時は `会議室`
  * `RESERVATION_MONTHS_AHEAD`
    * 何か月後まで予約可能にするか
    * 正の整数
    * 未設定時は `6`
  * `MAX_SLOTS_PER_REQUEST`
    * 一度に予約可能なコマ数
    * 正の整数
    * 未設定時は `50`
* frontend は backend の設定 API からアプリ設定を取得し、表示と画面上の制御に反映する
* 予約可能期間と一度に予約可能なコマ数の最終検証は backend で行う
* 最終的に Cloud Run に載せる
* frontend は public な Cloud Run service とする
* backend は Cloud Run IAM 認証を必須にする
* ブラウザは backend へ直接アクセスしない
* frontend の Node proxy が `/api/*` を backend に転送する
* frontend proxy は backend 呼び出し時に Cloud Run IAM 用 ID token を付ける
* backend は frontend の実行サービスアカウントからのみ呼び出し可能にする
* Cloud Build で frontend/backend をビルド、デプロイする
* Cloud Build Trigger により frontend/backend の変更を自動デプロイできる
* Cloud Run、Cloud Build、IAM、Artifact Registry などのインフラは Terraform で管理できる
